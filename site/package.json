{
  "name": "nexus-scholar-core-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "check": "astro check",
    "build": "astro build",
    "validate:dist": "node scripts/validate-dist.mjs",
    "preview": "astro preview",
    "verify": "npm run check && npm run build && npm run validate:dist"
  },
  "devDependencies": {
    "@astrojs/check": "0.9.9",
    "astro": "7.1.1",
    "typescript": "6.0.3"
  }
}
