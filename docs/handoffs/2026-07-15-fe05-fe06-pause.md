# FE-05 / FE-06 implementation pause

Last refreshed: 2026-07-16 01:41 +01:00

## Resume point

- Branch: `cdx/fe-05-local-fulltext`
- Base: merged FE-03/FE-04 main at `f9bbaf8`
- FE-05 commits already created:
  - `86a7881` Define FE-05 local Full Text authority
  - `4388c22` Add canonical Full Text extraction attempts
  - `463b75a` Add Full Text extraction conformance cases
  - `3f618a8` Add verified Full Text admission authority
  - `1d23771` Constrain Full Text screening bridge dependencies
  - `6553eb9` Add transactional Full Text conduct workspace slice
  - `d2f3778` Record FE-05 and FE-06 pause checkpoint
  - `35f64be` Harden Full Text workspace failure status
  - `bafc62b` Correct Full Text conduct replay authority

## Current checkpoint

- HEAD: `bafc62b` Correct Full Text conduct replay authority
- Branch: `cdx/fe-05-local-fulltext`
- Intentional uncommitted files:
  - `src/NexusScholar.Screening.FullText/NexusScholar.Screening.FullText.csproj`
  - `tests/NexusScholar.Architecture.Tests/DependencyRulesTests.cs`
- Those edits add explicit Protocol/Deduplication project references and admit
  those dependencies in the architecture allowlist. The focused architecture
  test passes.

## Implemented and committed

- Full-text human conduct policy/header/decision/invalidation/journal/handoff in
  `NexusScholar.Screening.FullText`.
- Ordered Full Text conduct entry replay and application-service preview/commit port.
- ResearchWorkspace Full Text pointer trio, canonical generation manifest,
  pointer-last transaction, exact inventory verification, raw-byte verification,
  additional conduct-record persistence, and workspace commit port.
- Focused end-to-end test covering human authority, handoff, source-scoped
  invalidation, workspace commit, reopen, and raw-byte tamper rejection.
- Strict canonical conduct codec and replay checks for policy binding,
  independent reviewers, correction/adjudication sources, artifact evidence,
  and complete source-scoped invalidation.
- Fault injection, stale-writer, extra-file, post-promotion quarantine, CLI
  status, and conduct-tamper coverage.

## Validation completed

- Release build: succeeded with zero warnings and zero errors.
- Full solution tests: 730 passed.
- Format verification: passed.
- Repository policy tests: 3 passed.
- Screening.FullText architecture boundary test: passed after the current
  uncommitted dependency/allowlist edits.
- Release-policy verification currently stops at package topology because the
  new packable `NexusScholar.Screening.FullText` project is not yet represented
  in `eng/package-topology.json` and package-smoke evidence.

## Next actions

1. Preserve and commit the two current dependency/topology-boundary edits.
2. Add `NexusScholar.Screening.FullText` to `eng/package-topology.json`, package
   smoke expectations, and any package-topology documentation required by the
   release policy.
3. Run `scripts/verify-release-policy.ps1` and `scripts/verify-packages.ps1`.
4. Update FE-05 gate evidence; repeat full build/test/format and affected
   architecture/conformance gates after the packaging changes.
5. Obtain independent scientific-invariant and test reviews; resolve findings.
6. Push FE-05, verify hosted CI, merge, and refresh `main`.
7. Start FE-06 only after FE-05 is merged: report conservation, RapidReview profile,
   verified deviations, Bundle v2 exact inventory, export ledger, CLI, fixtures, and gates.

## Important constraint

Do not treat FE-05 as complete yet. Behavior gates are green, but package
topology/release-policy verification, independent review, hosted CI, and merge
remain. FE-06 has not started.
