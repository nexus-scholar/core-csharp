# FE-05 / FE-06 implementation pause

## Resume point

- Branch: `cdx/fe-05-local-fulltext`
- Base: merged FE-03/FE-04 main at `f9bbaf8`
- FE-05 commits already created:
  - `86a7881` Define FE-05 local Full Text authority
  - `4388c22` Add canonical Full Text extraction attempts
  - `463b75a` Add Full Text extraction conformance cases
  - `3f618a8` Add verified Full Text admission authority
  - `1d23771` Constrain Full Text screening bridge dependencies

## Implemented but not yet committed

- Full-text human conduct policy/header/decision/invalidation/journal/handoff in
  `NexusScholar.Screening.FullText`.
- Ordered Full Text conduct entry replay and application-service preview/commit port.
- ResearchWorkspace Full Text pointer trio, canonical generation manifest,
  pointer-last transaction, exact inventory verification, raw-byte verification,
  additional conduct-record persistence, and workspace commit port.
- Focused end-to-end test covering human authority, handoff, source-scoped
  invalidation, workspace commit, reopen, and raw-byte tamper rejection.

## Validation completed

- Full Text authority/admission tests: 11 passed.
- Full Text conduct plus workspace focused test: passed.
- `NexusScholar.Screening.FullText` build: succeeded with zero warnings.
- `NexusScholar.ResearchWorkspace` build: succeeded with zero warnings.
- Screening.FullText architecture boundary test: passed.

## Next actions

1. Review and commit the current FE-05 conduct/workspace/application slice.
2. Add pointer-last fault-injection, stale-writer, extra-file, and conduct replay tests.
3. Add read-only CLI status output for the current Full Text generation.
4. Update FE-05 gate evidence and run full build/test/format plus architecture and package smoke gates.
5. Obtain independent review, push FE-05, verify hosted CI, and merge.
6. Start FE-06 only after FE-05 is merged: report conservation, RapidReview profile,
   verified deviations, Bundle v2 exact inventory, export ledger, CLI, fixtures, and gates.

## Important constraint

Do not treat FE-05 as complete yet. The current branch is intentionally paused
before fault-path coverage, CLI status, full gates, independent review, and merge.
