# Open Conflicts

Status: living conflict register. Items remain open until an ADR or explicit gate decision resolves them for a named scope.

| Id | Severity | Topic | Conflict | Owner or decision gate | Current effect |
| --- | --- | --- | --- | --- | --- |
| `CF-001` | implemented for Gate 3 local scope | Protocol contract | Decision accepted by `ADR 0003` and implemented locally for Gate 3 protocol lifecycle behavior: protocol records include draft, required decision definition, decision, unresolved decision, approved version, amendment, waiver, deviation, invalidation notice, and `protocol-content` digest rules. Blueprint and bundle conformance remain separate non-claims. | Spec owner + architecture owner, later blueprint/bundle gates | local Gate 3 protocol behavior is implemented; `protocol-minimal.json` remains discovery-only and blueprint/PHP parity remain unclaimed |
| `CF-002` | implemented for Gate 6 local scope | Bundle contract | Decision accepted by `ADR 0009` and implemented locally: review-bundle manifests use schema id `nexus.review-bundle.manifest`, schema version `1.0.0`, `bundle-manifest` digests, typed artifact entries, raw-byte artifact digests, known `protocol-content` and `provenance-event` envelope digest validation, stable tamper categories, staged verification, and destructive-overwrite rejection. | Spec owner, later blueprint/PHP/bundle adoption gates | local bundle/artifact behavior is implemented; blueprint conformance, PHP compatibility, persistence, API/UI/cloud sync, provider behavior, and workflow execution remain unclaimed |
| `CF-003` | implemented for Gate 4 local scope | Workflow compiler | Decision accepted by `ADR 0006` and implemented locally: workflow compilation is template-driven, deterministic, bound to an approved protocol version, and rejects duplicate nodes, missing dependencies, cycles, draft protocols, stale protocol digests, invalid approval gates, invalid waivers, invalid invalidation sources, and invalid hybrid nodes. | Workflow owner, later execution/provenance/bundle gates | local workflow compiler behavior is implemented; workflow execution, provenance, bundles, blueprint conformance, and PHP compatibility remain unclaimed |
| `CF-004` | resolved for Gate 5 local provenance scope; AI governance remains blocking for Gate 11 | Provenance and AI governance | Decision accepted by `ADR 0008`: local provenance events record agent, activity, subject, protocol/workflow binding, input/output digest references, event digest, and append-only in-memory ledger ordering. AI governance, context manifests, evidence policy, provider behavior, and AI task records remain unresolved. | Governance owner, Gate 5 for provenance ledger and Gate 11 for governed AI | local provenance ledger behavior is implemented; governed AI remains unresolved and no blueprint, bundle, persistence, provider, or PHP parity is claimed |
| `CF-005` | blocking | Blueprint authority | Blueprint markdown specs, JSON schemas, contract sketches, and templates drift from each other. Authority order is not frozen. | Spec owner + architecture owner, Gate 0 exit prerequisite for later adoption | blueprint is a discovery input only |
| `CF-006` | implemented for Gate 4 local scope | Missing schema closure | Decision accepted by `ADR 0005` and implemented locally: any template compiled by this repository must be schema-closed for its authoritative fixture set; missing or unknown schema ids and missing schema versions are validation failures, and local placeholder schemas do not create blueprint conformance claims. | Artifact/schema registry owner, later blueprint adoption gates | blueprint schema gaps remain discovery findings; Gate 4 local fixtures list schema refs explicitly |
| `CF-007` | implemented for Gate 4 local scope | `hybrid` workflow mode | Decision accepted by `ADR 0006` and implemented locally: `hybrid` means human-directed work with declared automation/plugin/AI capability requirements plus explicit human review or approval semantics; automation can propose or validate but cannot authorize scientific conduct. | Workflow owner, later AI/plugin/governance gates | local hybrid-node validation is implemented; AI execution, plugin runtime, and governance parity remain future work |
| `CF-008` | implemented for Gate 3 local scope | Approval semantics | Decision accepted by `ADR 0004` and implemented locally for Gate 3 protocol approval: approval binds human actor, timestamp, version id, exact content digest, approval-record digest, and explicit policy; single and dual-independent policies are supported, with automation rejection and same-actor dual rejection. | Governance owner, later workflow/AI/plugin/institutional approval gates | protocol approval semantics are implemented locally; workflow, AI, plugin, and institutional approval engines remain future work |
| `CF-009` | important | Canonical JSON and digest scope | Narrowed by `ADR 0002`: Gate 2 local deterministic-kernel rules now define canonical JSON profile, digest rendering, digest scope, timestamp rules, Unicode normalization, omitted-vs-null semantics, and fixture expectations. Remaining protocol and bundle digest-shape questions stay under `CF-001` and `CF-002`. | Spec owner, before Gate 2 implementation and fixtures | Gate 2 may implement local deterministic-kernel digest rules without claiming blueprint or PHP parity |
| `CF-010` | implemented for Gate 9 local scope | Scientific identity fallback | Decision accepted by `ADR 0007` and implemented locally: C# rejects PHP `spl_object_hash` / `spl_object_id` fallback as scientific identity, uses normalized stable identifier overlap for work identity, treats no-primary-id works as unresolved candidates, and forbids title-only or runtime-object fallback dedupe. | Porting owner, later PHP compatibility comparator gate | local shared identity behavior is implemented; PHP compatibility remains unclaimed until generator-backed fixtures and comparators exist, and the object-id fallback remains an intentional incompatibility |
| `CF-011` | implemented for Gate 9 local dedup scope | Raw duplicate input shape | Decision accepted by `ADR 0012`: C# Deduplication consumes raw Search trace/import sighting evidence with source trace, sighting, import source, source record, source-file digest, source-file digest scope, raw-record digest, parser warning, and record-notice bindings where present. PHP's pre-deduplicated Search corpus is not authoritative C# Deduplication input. | Porting owner, local Dedup implementation | local Dedup consumes raw evidence as input; PHP compatibility and generated comparator fixtures remain unclaimed |
| `CF-012` | implemented for Gate 9 local dedup scope | Title fuzzy threshold | Decision accepted by `ADR 0012`: the local default fuzzy title threshold is `95` / `0.95`; PHP `92` is treated as documentation/configuration drift unless an explicit custom policy records otherwise. | Porting owner, later PHP comparator gate | local Dedup now uses threshold `95` / `0.95`; PHP parity cannot be claimed until generated fixtures and comparators classify the drift |
| `CF-013` | implemented for local Search scope | Search cache identity | C# Search keeps provider-order-insensitive cache identity and intentionally includes `include_raw_data` because raw payload preservation changes the Search trace evidence surface. PHP excludes `includeRawData`; that difference is an intentional incompatibility unless a later ADR reverses it. | Porting owner, later PHP comparator gate | local stub-provider Search implements the rule; PHP compatibility remains unclaimed until generated fixtures and comparators classify the difference |
| `CF-014` | Gate 6 equality implemented; H27 compatibility boundary narrowed by `ADR 0026`; broader snapshot semantics unresolved | Snapshot identity and equality | `ADR 0009` defines local bundle round-trip equality. `ADR 0026` allows H27 to retain PHP lock/snapshot observations as intentional non-adoption evidence without treating Research Workspace locks or app persistence as Core corpus snapshot authority. | Spec owner + porting owner, later dedicated corpus snapshot gate | H27 may classify generated PHP lock/snapshot cases as intentional changes; general corpus snapshot identity/equality, C# implementation, PHP persistence parity, Search, Deduplication, and Screening snapshot semantics remain unresolved |
| `CF-015` | important | Process completeness | The Gate 0 prompt requires discovery, conflict, fixture, and port-planning documents that were absent before this pass. | Gate 0 document owner, resolved by this document set | Gate 0 was incomplete before documentation freeze |
| `CF-016` | implemented for Search scope; implemented locally for Dedup handoff by `ADR 0012` | Search and Deduplication boundary | C# Search output is a raw Search trace, preserves duplicate provider sightings, keeps no-id sightings as unresolved candidates, and does not call Deduplication. `ADR 0012` defines that Deduplication consumes those Search/import sightings later. PHP Search-time deduplication remains an intentional local incompatibility. | Porting owner, Dedup implementation gate | Search now outputs raw traces; Deduplication now groups those raw traces locally without changing Search behavior |
| `CF-017` | implemented for local Search scope | Search plan schema closure | Authoritative local C# Search plan artifacts are schema-closed and reject unknown fields, missing schema id, missing schema version, and unsupported schema version. PHP-permissive plan parsing is allowed only through an explicit legacy import/comparator profile. | Porting owner + schema owner, later PHP comparator gate | local stub-provider Search implements schema-closed plans; PHP compatibility remains unclaimed until generated fixtures and comparators classify permissive parsing differences |
| `CF-018` | narrowed for Search consumer boundary by `ADR 0010`; broader app alignment pending | App consumer boundary | CLI/Web may consume Search traces and display projections, but app display hashes, fallback keys, run files, latest pointers, database rows, job lifecycle rows, audit rows, wiki pages, app snapshots, and app manifests are not Core authority. | App-alignment owner + porting owner, before app integration claims | Search can expose raw traces consumable by apps without Search-time dedupe; later Deduplication, Screening, Protocol, Provenance, Bundle, Snapshot, API/UI/cloud, and AI governance gates must map or reject app projections explicitly |
| `CF-019` | implemented for local first-slice import behavior only | Imported Search Source Contract | Decision accepted by `ADR 0011`: imported-export means user-supplied external search export evidence, not live provider integration. Imported local records now support RIS/BibTeX/Scopus CSV parsing with source database/tool, export format, parser id/version, raw source file digest, local import timestamp and actor, parser warnings, and source-specific identifier evidence with Search trace projection. | Porting owner + Search import owner + local import implementation owner | source-specific namespace expansion, remaining parser families, PHP compatibility, live provider/API integration, Scopus/Web of Science APIs, Google Scholar scraping, Deduplication, Screening, persistence, and app alignment remain future |
| `CF-020` | narrowed for Core by `ADR 0012` | Deduplication app projection and representative snapshot boundary | Nexus Web adds membership hashes, persisted dedup runs, exact-identifier grouping over persisted rows, representative scoring fallback, stale run detection, and representative-aware corpus lock snapshots. `ADR 0012` treats these as integration evidence and app projections, not Core Deduplication authority. | Porting owner + app-alignment owner, later app/snapshot/persistence gates | local Dedup implementation may proceed without adopting app hashes, app persisted runs, app representative scoring, or app snapshots as Core authority |
| `CF-021` | implemented for local Screening scope by `ADR 0013` | Screening input and locked candidate boundary | PHP Screening can read project works through `ScreeningWorkSourcePort` and optional `CorpusLockPolicy`; Nexus Web requires representative-aware locked snapshots and full-text handoff artifacts. `ADR 0013` decides that C# Screening consumes a Deduplication result or locked/reviewable candidate set and must not consume raw Search traces directly. | Porting owner + Screening implementation owner, local Screening implementation | local Screening input and lock boundary is implemented; general corpus snapshot equality, PHP compatibility, and app snapshot authority remain future |
| `CF-022` | implemented for local Screening scope by `ADR 0013` | Screening decision authority and AI proposal boundary | PHP records LLM single/council outputs as screening verdicts, while C# product laws say LLM outputs remain proposals until authorized human action accepts them. `ADR 0013` decides that final Screening decisions require an identified human actor and AI/model/rule outputs are proposal evidence only. | Governance owner + Screening implementation owner, local Screening implementation | local screening human-authority and AI-proposal boundaries are implemented; AI governance remains future |
| `CF-023` | implemented for local Screening scope by `ADR 0013` | Screening criteria schema and digest contract | PHP criteria hash recursively sorts associative keys and hashes JSON with SHA-256, but it has no `ADR 0002` digest scope, schema id, protocol binding, or stage-specific schema. `ADR 0013` defines `nexus.screening.criteria` version `1.0.0` and `ADR 0002` canonical JSON record digest binding. | Screening implementation owner + kernel/schema owner, local Screening implementation | local Screening criteria digest contract is implemented with approved protocol version id and `protocol-content` digest binding; PHP hash compatibility remains unclaimed |
| `CF-024` | narrowed for Core by `ADR 0013` | Screening app workflow projection boundary | CLI file screening outputs and Nexus Web batches, assignments, conflicts, full-text item links, and audit rows are user-visible workflow evidence, but they are app projections unless a C# Screening ADR admits them as Core records. `ADR 0013` admits Core decision/conflict/adjudication semantics only and leaves app workflow rows as projections. | App-alignment owner + Screening implementation owner, later app alignment | CF-024 remains narrowed for Core; local implementation proceeds without adopting app assignment, batch, conflict-row, full-text item, or audit-row authority |
| `CF-025` | implemented/resolved for local Full Text artifact evidence | Full Text artifact evidence and raw-byte identity | Decision accepted by `ADR 0014` and implemented locally: Full Text artifact evidence uses exact accepted bytes plus `raw-artifact-bytes` digest under `nexus.fulltext.artifact-evidence`; missing digest, wrong scope, digest mismatch, local path identity, and app projection authority are rejected by stable categories. | Full Text owner + artifact owner, local Full Text implementation | local no-network Full Text artifact evidence is implemented; PHP path-based records remain compatibility evidence only and PHP compatibility is unclaimed |
| `CF-026` | narrowed by `ADR 0014`; live behavior still blocked | Full Text provider/network and legal-access boundary | Decision accepted by `ADR 0014` and implemented only for the no-network slice: user-supplied bytes, deterministic stub artifacts, manual acquisition records, source-reference metadata, source attempts, and validation. Live providers, HTTP downloads, provider SDKs, credentials, scraping, paywall bypass, and shadow-library sources remain blocked. | Full Text owner + provider/network/legal gate | local no-network Full Text implementation is complete for first slice; live retrieval and API/provider integration remain future work |
| `CF-027` | narrowed for Core by `ADR 0014`; projection boundary preserved | Full Text app projection and Screening handoff boundary | Decision accepted by `ADR 0014` and implemented locally: full-text Screening handoff uses digest-bound artifact or extraction evidence; CLI manifests, Web batches/items, `pdf_fetches`, app audit rows, download routes, source-full-text item ids, storage paths, and app row ids remain projections unless transformed into ADR 0014 records later. | App-alignment owner + Full Text owner, local Full Text implementation and later app alignment | app rows may guide adapters and fixtures but do not become Core authority; `ADR 0013` full_text decisions cite `ADR 0014` evidence refs |
