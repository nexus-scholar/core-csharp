# Search Provider Parser Samples

These JSON files are synthetic, deterministic parser samples. They exercise
OpenAlex and Semantic Scholar response shapes, malformed inputs, paging, and
identifier handling.

They are not retained provider responses, golden compatibility fixtures, or
evidence of current provider behavior. Runtime evidence created from these
bytes exists only inside tests and makes no provider compatibility claim.
